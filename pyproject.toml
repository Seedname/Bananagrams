[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "banana"
version = "0.1.0"
dependencies = [
    "tomli_w",
    "matplotlib",
    "scipy",
]

[tool.setuptools.packages.find]
where = ["."]